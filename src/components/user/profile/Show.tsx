import React from 'react';
import './Show.css'

interface IProps {
    isAuthenticated: boolean,
    currentUser: any
}

export default (props: IProps) => {
    return (
        <div>
            Profile
        </div>
    )
}